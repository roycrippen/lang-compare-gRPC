# language compare using gRPC configuration

# servers providing the requested function execution
servers:
  - py:   # python gRPC server
      port: 50052
      server: lang_compare_server.py
  - cpp:  # cpp gRPC server
      port: 50051
      server: ../cpp/lang_compare_server

# test runner to compare function results from multiple languages
runners:
  - xor_cipher_py_cpp:
      type: py
      prog: xor_cipher_compare.py
      langs:
        - py
        - cpp
  - test_servers:
      type: py
      prog: test_servers.py
      langs:
        - py
        - cpp

# list of runs to execute
run:
#  - xor_cipher_py_cpp
  - test_servers

